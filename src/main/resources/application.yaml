spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://${DB_HOST:postgres_db}:${DB_PORT:5432}/${DB_NAME:raizes_nordeste}
    username: ${DB_USER:user}
    password: ${DB_PASSWORD:password}
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            scripts:
#              action: create
#              create-target: src/main/resources/db/migration/V1_init.sql
    flyway:
      enabled: true
      locations: classpath:db/migrations
jwt:
  # random fallback value in case you missed
  secret: ${SECRET:2bd3855646db537030105d119dbdf37d39b16fb174dafbaa1424d8441b1671a9}
admin:
  password: ${ADMIN_PASSWORD:password}